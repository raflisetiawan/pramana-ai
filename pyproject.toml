# =============================================================
# Pramana AI — Project Configuration
# =============================================================

[tool.pytest.ini_options]
pythonpath = ["services/mock-vclaim"]
testpaths = ["tests"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
]
